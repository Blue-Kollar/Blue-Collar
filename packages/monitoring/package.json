{
  "name": "@bluecollar/monitoring",
  "version": "1.0.0",
  "description": "Contract monitoring and alerting service for BlueCollar",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@bluecollar/test-utils": "workspace:*",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^3.2.7",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.6"
  }
}
